# Greenhouse Docker Environment Configuration
# Copiar este archivo a .env (local) o .env.production (VPS)
# NO commitear archivos .env con valores reales

# ========== NODE CONFIGURATION ==========
PORT=8080
NODE_ENV=development

# ========== DATABASE ==========
# Local: mongodb://mongodb:27017/greenhouse
# Docker: usa hostname "mongodb" (no localhost)
MONGODB_URI=mongodb://mongodb:27017/greenhouse

# ========== CORS ==========
# Local development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# Production (comentar arriba, descomentar abajo)
# ALLOWED_ORIGINS=https://reimon.dev

# ========== RATE LIMITING ==========
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000

# ========== ESP32 AUTHENTICATION ==========
# IMPORTANTE: Generar con: openssl rand -hex 32
# Mínimo 32 caracteres requerido
# Este token debe coincidir con API_TOKEN en esp32-firmware/include/secrets.h
ESP32_AUTH_TOKEN=dev_token_minimum_32_characters_required_please_change_me_asap

# ========== LOGGING ==========
LOG_LEVEL=debug

# ========== DOCKER IMAGE (VPS solo) ==========
# DOCKER_IMAGE=tu-usuario/greenhouse:latest
