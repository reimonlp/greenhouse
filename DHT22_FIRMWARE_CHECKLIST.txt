╔════════════════════════════════════════════════════════════════════════════════╗
║                 DHT22 FIRMWARE UPDATE - LISTA DE VERIFICACIÓN                  ║
║                           ESP32-C3 Super Mini                                  ║
╚════════════════════════════════════════════════════════════════════════════════╝

📋 ESTADO ACTUAL:
✅ Código compilado exitosamente (73.0% flash)
✅ Cambios confirmados: DHT22 configuration
✅ GPIO verification completada
✅ WebSocket conectando correctamente

🔧 CAMBIOS REALIZADOS:
  • DHT_TYPE: DHT11 → DHT22
  • Rango temperatura: 0-50°C → -40 a 80°C
  • Rango humedad: 20-90% → 0-100%
  • Tiempos de estabilización aumentados para DHT22
  • Thresholds de anomalías ajustados

⏭️ PRÓXIMOS PASOS (cuando conectes ESP32):

1. CONECTAR ESP32-C3 POR USB
   $ ls -la /dev/ttyACM*  (debería aparecer /dev/ttyACM0)

2. SUBIR FIRMWARE
   $ cd /home/reimon/greenhouse/esp32-firmware
   $ pio run --target upload

3. MONITOREAR SERIAL
   $ pio device monitor --port /dev/ttyACM0 --baud 115200

4. QUÉ ESPERAR EN LA SALIDA:
   ✓ WiFi conectado
   ✓ Tiempo sincronizado (NTP)
   ✓ OTA ready
   ✓ WebSocket connected
   ✓ [DHT22] Valid: XX.X°C, XX.X% ← NUEVA SALIDA CON DHT22

5. SI DHT22 SIGUE SIN FUNCIONAR:
   • Verifica conexión en GPIO1
   • Comprueba voltaje 3.3V en sensor
   • Revisa GND común
   • Opcional: agregar capacitor 100nF entre VCC y GND del sensor

═══════════════════════════════════════════════════════════════════════════════════

COMPARATIVA DHT11 vs DHT22:

┌──────────────────┬─────────────────┬──────────────────┐
│ Parámetro        │ DHT11           │ DHT22 (AM2302)   │
├──────────────────┼─────────────────┼──────────────────┤
│ Temp. Range      │ 0 a 50°C        │ -40 a 80°C       │
│ Temp. Accuracy   │ ±2°C            │ ±0.5°C           │
│ Humidity Range   │ 20 a 90%        │ 0 a 100%         │
│ Humidity Acc.    │ ±5%             │ ±2%              │
│ Response Time    │ ~1s             │ ~2s              │
│ Interface        │ Single wire     │ Single wire      │
│ Precio           │ ~$1-2           │ ~$3-5            │
└──────────────────┴─────────────────┴──────────────────┘

═══════════════════════════════════════════════════════════════════════════════════

COMANDO RÁPIDO PARA COMPILAR Y SUBIR:

cd /home/reimon/greenhouse/esp32-firmware && pio run --target upload && pio device monitor --port /dev/ttyACM0 --baud 115200

═══════════════════════════════════════════════════════════════════════════════════
